// Converted from next.config.ts -> next.config.mjs
// Remove any TypeScript-only types or imports before adding extra options.

const nextConfig = {
  // Add your config options here. Example:
  reactStrictMode: true,
  // swcMinify: true,
  // images: { domains: ['example.com'] },
  // async redirects() { return [ /* ... */ },
};

export default nextConfig;
